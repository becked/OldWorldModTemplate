name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  powershell-windows:
    name: PowerShell (Windows)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install PSScriptAnalyzer
        shell: pwsh
        run: Install-Module -Name PSScriptAnalyzer -Force -Scope CurrentUser

      - name: Lint PowerShell scripts
        shell: pwsh
        run: |
          $results = @()
          $results += Invoke-ScriptAnalyzer -Path template/scripts/*.ps1 -Settings PSGallery
          $results += Invoke-ScriptAnalyzer -Path create-mod.ps1 -Settings PSGallery
          if ($results) {
            $results | Format-Table -AutoSize
            throw "PSScriptAnalyzer found $($results.Count) issue(s)"
          }
          Write-Host "PSScriptAnalyzer: no issues found"

      - name: Install Pester
        shell: pwsh
        run: Install-Module -Name Pester -Force -Scope CurrentUser -MinimumVersion 5.0 -SkipPublisherCheck

      - name: Run Pester tests
        shell: pwsh
        run: |
          $config = New-PesterConfiguration
          $config.Run.Path = 'tests'
          $config.Output.Verbosity = 'Detailed'
          $config.Run.Exit = $true
          Invoke-Pester -Configuration $config

  bash-ubuntu:
    name: Bash (Ubuntu)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install xmllint
        run: sudo apt-get update && sudo apt-get install -y libxml2-utils

      - name: Run validate.sh on template
        run: ./scripts/validate.sh
        working-directory: template

      - name: Test write_modinfo_platform
        run: |
          cd "$(mktemp -d)"
          cat > ModInfo.xml <<'EOF'
          <?xml version="1.0"?>
          <ModInfo>
            <displayName>Test</displayName>
            <author>tester</author>
            <modversion>1.0</modversion>
            <singlePlayer>true</singlePlayer>
            <multiplayer>false</multiplayer>
          </ModInfo>
          EOF

          source "$GITHUB_WORKSPACE/template/scripts/helpers.sh"
          write_modinfo_platform ModInfo.xml "Modio" "12345" "" "1.0.83082"

          # Each platform tag must be on its own line (regression for the
          # BSD-sed \n-as-literal-"n" bug that put all tags on one line).
          grep -q '^  <modplatform>Modio</modplatform>$' ModInfo.xml
          grep -q '^  <modioID>12345</modioID>$' ModInfo.xml
          grep -q '^  <modioFileID>0</modioFileID>$' ModInfo.xml
          grep -q '^  <modbuild>1.0.83082</modbuild>$' ModInfo.xml
          ! grep -q '<workshopFileID>' ModInfo.xml

          xmllint --noout ModInfo.xml

          # Idempotent: re-running keeps exactly one of each tag.
          write_modinfo_platform ModInfo.xml "Modio" "12345" "" "1.0.83082"
          test "$(grep -c '<modplatform>' ModInfo.xml)" = "1"
          test "$(grep -c '<modioID>' ModInfo.xml)" = "1"
          test "$(grep -c '<modbuild>' ModInfo.xml)" = "1"

          # Workshop variant
          write_modinfo_platform ModInfo.xml "Workshop" "" "999888" "1.0.83082"
          grep -q '^  <modplatform>Workshop</modplatform>$' ModInfo.xml
          grep -q '^  <workshopFileID>999888</workshopFileID>$' ModInfo.xml
          ! grep -q '<modioID>' ModInfo.xml
          xmllint --noout ModInfo.xml

          echo "write_modinfo_platform: PASSED"

      - name: Test bump-version.sh
        run: |
          ./scripts/bump-version.sh patch
          grep '<modversion>0.1.1</modversion>' ModInfo.xml
          git checkout -- ModInfo.xml CHANGELOG.md
        working-directory: template

      - name: Test create-mod.sh (XML-only)
        run: |
          cd "$(mktemp -d)"
          MOD_NAME="Test Mod" AUTHOR="CI Bot" MOD_TYPE=xml \
            TEMPLATE_DIR="$GITHUB_WORKSPACE/template" \
            bash "$GITHUB_WORKSPACE/create-mod.sh"

          # Verify folder created with PascalCase name
          test -d TestMod

          # Verify placeholder replacement in ModInfo.xml
          grep '<displayName>Test Mod</displayName>' TestMod/ModInfo.xml
          grep '<author>CI Bot</author>' TestMod/ModInfo.xml

          # Verify workshop.vdf title
          grep '"Test Mod"' TestMod/workshop.vdf

          # Verify gitignore renamed
          test -f TestMod/.gitignore
          test ! -f TestMod/gitignore

          # Verify C# files removed for XML-only
          test ! -f TestMod/MyMod.csproj
          test ! -d TestMod/Source

          # Verify bin/obj stripped from .gitignore
          ! grep '^bin/' TestMod/.gitignore
          ! grep '^obj/' TestMod/.gitignore

          # Verify Infos/ preserved
          test -d TestMod/Infos

          echo "create-mod.sh XML-only: PASSED"

      - name: Test create-mod.sh (C#)
        run: |
          cd "$(mktemp -d)"
          MOD_NAME="Cool Strategy Mod" AUTHOR="CI Bot" MOD_TYPE=csharp \
            TEMPLATE_DIR="$GITHUB_WORKSPACE/template" \
            bash "$GITHUB_WORKSPACE/create-mod.sh"

          # Verify PascalCase folder
          test -d CoolStrategyMod

          # Verify .csproj renamed and configured
          test -f CoolStrategyMod/CoolStrategyMod.csproj
          test ! -f CoolStrategyMod/MyMod.csproj
          grep '<AssemblyName>CoolStrategyMod</AssemblyName>' CoolStrategyMod/CoolStrategyMod.csproj
          grep '<RootNamespace>CoolStrategyMod</RootNamespace>' CoolStrategyMod/CoolStrategyMod.csproj

          # Verify ModEntryPoint.cs configured
          grep 'namespace CoolStrategyMod' CoolStrategyMod/Source/ModEntryPoint.cs
          grep 'com\.cibot\.coolstrategymod' CoolStrategyMod/Source/ModEntryPoint.cs
          grep '\[CoolStrategyMod\]' CoolStrategyMod/Source/ModEntryPoint.cs
          ! grep 'namespace MyMod' CoolStrategyMod/Source/ModEntryPoint.cs
          ! grep '\[MyMod\]' CoolStrategyMod/Source/ModEntryPoint.cs

          # Verify .gitignore retains bin/obj
          grep '^bin/' CoolStrategyMod/.gitignore
          grep '^obj/' CoolStrategyMod/.gitignore

          echo "create-mod.sh C#: PASSED"

      - name: Test create-mod.sh (monorepo, C#)
        run: |
          cd "$(mktemp -d)"
          mkdir -p mods scripts
          touch scripts/helpers.sh

          MOD_NAME="Monorepo Mod" AUTHOR="CI Bot" MOD_TYPE=csharp \
            TEMPLATE_DIR="$GITHUB_WORKSPACE/template" \
            bash "$GITHUB_WORKSPACE/create-mod.sh" --monorepo .

          # Verify created in mods/
          test -d mods/MonorepoMod

          # Verify mod-specific files exist
          test -f mods/MonorepoMod/ModInfo.xml
          test -f mods/MonorepoMod/MonorepoMod.csproj
          test -d mods/MonorepoMod/Source
          test -d mods/MonorepoMod/Infos
          test -f mods/MonorepoMod/CLAUDE.md
          test -f mods/MonorepoMod/.env
          test -f mods/MonorepoMod/CHANGELOG.md
          test -f mods/MonorepoMod/workshop.vdf
          test -f mods/MonorepoMod/mod-description.html

          # Verify shared infrastructure NOT present
          test ! -d mods/MonorepoMod/scripts
          test ! -d mods/MonorepoMod/docs
          test ! -f mods/MonorepoMod/.env.example
          test ! -f mods/MonorepoMod/.gitignore
          test ! -f mods/MonorepoMod/gitignore

          # Verify per-mod .env has correct content
          grep 'STEAM_WORKSHOP_ID' mods/MonorepoMod/.env
          grep 'MODIO_MOD_ID' mods/MonorepoMod/.env

          # Verify placeholder replacement still works
          grep '<displayName>Monorepo Mod</displayName>' mods/MonorepoMod/ModInfo.xml

          echo "create-mod.sh monorepo C#: PASSED"

      - name: Test create-mod.sh (monorepo, XML-only)
        run: |
          cd "$(mktemp -d)"
          mkdir -p mods scripts
          touch scripts/helpers.sh

          MOD_NAME="My Mono Mod" AUTHOR="CI Bot" MOD_TYPE=xml \
            TEMPLATE_DIR="$GITHUB_WORKSPACE/template" \
            bash "$GITHUB_WORKSPACE/create-mod.sh" --monorepo .

          test -d mods/MyMonoMod
          test ! -f mods/MyMonoMod/MyMod.csproj
          test ! -d mods/MyMonoMod/Source
          test -d mods/MyMonoMod/Infos
          test -f mods/MyMonoMod/CLAUDE.md
          test -f mods/MyMonoMod/.env

          echo "create-mod.sh monorepo XML: PASSED"

      - name: Test create-mod.sh rejects existing directory
        run: |
          cd "$(mktemp -d)"
          mkdir TestMod
          if MOD_NAME="Test Mod" MOD_TYPE=xml \
            TEMPLATE_DIR="$GITHUB_WORKSPACE/template" \
            bash "$GITHUB_WORKSPACE/create-mod.sh" 2>/dev/null; then
            echo "FAIL: should have exited with error"
            exit 1
          fi
          echo "create-mod.sh duplicate dir check: PASSED"
